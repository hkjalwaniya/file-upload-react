const styles = (theme) => ({
    input:{
        borderRadius: '5px',
        border: '1px solid grey',
        padding: '6px 8px',
        boxShadow: 'inset 0px 2px 4px rgba(0, 0, 0, 0.3)',
        fontSize: '0.8rem'
    },
    focused: {
        borderColor: 'red'
    }
  })
  
  export default styles
  